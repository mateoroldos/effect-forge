[tools]
bun = "1.4.0"
node = "24"

[env]
ALCHEMY_PROFILE = "effect-forge"
ALCHEMY_STAGE = "dev_{{ env.USER }}_{{ config_root | basename }}"

[tasks.setup]
description = "Bootstrap a fresh checkout"
run = ["mise install", "bun install"]
