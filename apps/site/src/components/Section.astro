---
type Props = { readonly title: string };

const { title } = Astro.props;
---

<section class="flex flex-col gap-6">
  <h2 class="font-serif text-[1.75rem]/tight tracking-tight text-balance sm:text-[2rem]">{title}</h2>
  <slot />
</section>
